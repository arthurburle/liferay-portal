{{- if .Values.enabled -}}
{{- range $ext := .Values.extensions }}
---
apiVersion: v1
kind: ConfigMap
metadata:
    labels:
        {{- include "extLabels" (dict "root" $ "ext" $ext) | nindent 8 }}
    name: {{ printf "%s-ext-init-metadata" $ext.name }}
    namespace: {{ $.Release.Namespace }}
data:
    {{- range $k, $v := (dig "extensionMetadata" dict $ext) }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
{{- end }}
{{- end -}}